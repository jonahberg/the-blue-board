/**
 * Stats — ported in Task 8.
 *
 * Registered in `src/app/tabs.ts` from the start so routing, deep links and the mobile nav
 * are already wired; Task 8 replaces this file and nothing else.
 */

import { Placeholder } from './Placeholder';

export default function StatsView() {
  return <Placeholder title="Stats" summary="Fleet utilisation, phase breakdown, the hub matrix and top routes arrive with the Stats port." />;
}
