/**
 * Starlink — ported in Task 6.
 *
 * Registered in `src/app/tabs.ts` from the start so routing, deep links and the mobile nav
 * are already wired; Task 6 replaces this file and nothing else.
 */

import { Placeholder } from './Placeholder';

export default function StarlinkView() {
  return <Placeholder title="Starlink" summary="Starlink rollout pace, the hub departures board and the tail roster arrive with the Starlink port." />;
}
