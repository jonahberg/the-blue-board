/**
 * My Flights — ported in Task 7.
 *
 * Registered in `src/app/tabs.ts` from the start so routing, deep links and the mobile nav
 * are already wired; Task 7 replaces this file and nothing else.
 */

import { Placeholder } from './Placeholder';

export default function MyFlightsView() {
  return <Placeholder title="My Flights" summary="Your watched flights, live status countdowns, delay risk, journey history and connection checks arrive with the My Flights port." />;
}
