/**
 * Fleet — ported in Task 5.
 *
 * Registered in `src/app/tabs.ts` from the start so routing, deep links and the mobile nav
 * are already wired; Task 5 replaces this file and nothing else.
 */

import { Placeholder } from './Placeholder';

export default function FleetView() {
  return <Placeholder title="Fleet" summary="The searchable United mainline fleet database, utilisation charts and the aircraft detail dialog arrive with the Fleet port." />;
}
