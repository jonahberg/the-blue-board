/**
 * Aircraft detail dialog — ported in Task 5.
 *
 * Mounted by `Dashboard.tsx` from the start and rendering nothing until then, so Task 5
 * replaces this file alone: no change to the root, the tab registry or the state providers.
 * Opens from `useUi().aircraftReg`, which the flight sheet and the fleet table already set.
 */

export default function AircraftDetailDialog() {
  return null;
}
