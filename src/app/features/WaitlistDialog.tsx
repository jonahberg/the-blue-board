/**
 * Waitlist / feature-request dialog — ported in Task 8.
 *
 * Mounted by `Dashboard.tsx` from the start and rendering nothing until then, so Task 8
 * replaces this file alone: no change to the root, the tab registry or the state providers.
 * Opens from `useUi().waitlistOpen`, which the ?waitlist=1 deep link already sets.
 */

export default function WaitlistDialog() {
  return null;
}
