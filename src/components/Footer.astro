---
/**
 * Compatibility shim. The real footer is `site/SiteFooter.astro`; this wrapper
 * stays only so the layouts and pages that have not moved to `BaseLayout` yet
 * keep rendering a footer. It is deleted once nothing imports it.
 */
import SiteFooter from './site/SiteFooter.astro';
---
<SiteFooter />
