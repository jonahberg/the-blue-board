# SDD ledger — plan: docs/superpowers/plans/2026-09-11-shadcn-rebuild.md

Spec: docs/superpowers/specs/2026-09-11-shadcn-rebuild-design.md (reachable). Worktree: ~/bb-feat/shadcn-rebuild, branch feat/shadcn-rebuild, base 06b77a7 (main v1.7.24). Docs commits 5d4eec3, +1.

## Pre-flight conflict scan

| Pair / task | Produces vs consumes | Finding | Ruling |
|---|---|---|---|
| T0 ↔ T1 | T0 edits astro.config/tsconfig/package.json/global.css/site components; T1 edits src/lib, tests, main.js imports | Disjoint files; both touch `package.json`? T1 does not. | Run in parallel; T1 in an isolated worktree, merged by the controller. |
| T0 → T2 | BaseLayout props, global.css tokens, shadcn ui dir | T2 index.astro must use the dashboard variant (no SiteHeader) — BaseLayout needs a `variant` or `hideHeader` prop not listed in T0 interfaces | Ruling: add `hideSiteHeader?: boolean` to BaseLayout props in T0's dispatch — cheap; cost if wrong: T2 adds it itself. |
| T1 → T2..T8 | lib module signatures | T2 consumes `ticker.js`, `live-stats.js`, `plane-icon.js`, `global-search.js`, `home-airport.js`, `hub-health.js`; later tasks consume the rest | Signatures fixed in T1's table; implementers may add optional params but not rename. |
| T2 ↔ T3..T8 | tabs.ts registry + state stores | Parallel tasks each replace their own view file and fill their own store; shared files (tabs.ts, Dashboard.tsx, ui.tsx) may need small edits (e.g. dialogs mounted at root) | Ruling: T2 must pre-mount dialog slots (`AircraftDetailDialog`, `DelayExplainDialog`, `Fr24LookupDialog`, engagement surfaces) as placeholder components in Dashboard.tsx so T5/T7/T8 only replace files. Cost if wrong: merge conflicts resolved by controller. |
| T2 test retargeting ↔ T8 sw.test | T2 leaves sw.test untouched; T8 rewrites sw.js + test | No conflict. | — |
| T2 ↔ T9 | T2 moves public/index.html to legacy/; T9 edits layouts/pages | Disjoint. `build-metadata.test.js` touched by both? T2 changes `homeLastmodPaths`; T9 keeps layout paths | Ruling: T9 must not rename layout files (test pins literal paths). |
| T9 ↔ T10 | Footer.astro deletion (T9) vs trackers components importing Footer (T10) | TrackerDetailLayout imports Footer.astro; if T9 deletes it before T10 lands, build breaks | Ruling: T9 keeps `src/components/Footer.astro` as a thin re-export of SiteFooter; T12 deletes it. |
| T11 ↔ T2/T9 | TSA references in index brief (T2) and HubLayout (T9) | Both remove their own TSA bits early; T11 does the rest | Ordered: T11 after T2 and T9. |
| Skill rule "never dispatch implementers in parallel" vs plan parallelism | — | Conflict with the skill's process rule | Ruling: parallel implementers ONLY in separate git worktrees (Agent isolation=worktree) on disjoint file sets; controller merges each branch into feat/shadcn-rebuild and runs gates after each merge. Cost if wrong: merge conflicts / rework. |
| Global constraint "never delete a test" vs T12 deleting `tests/main-js-imports.test.js` | — | That test exists only to guard the strangler period | Ruling: allowed; it is a scaffolding test created by T1. |
| T2 dev proxy (`/api` → prod) vs CLAUDE.md dev workflow | — | Dev/preview need live APIs without secrets | Ruling: proxy only in `astro.config` `vite.server.proxy` and `preview` when `!process.env.VERCEL`; never in production output. |

Per-task self-consistency: T0 (privacy conversion + sitemap add) ✓; T1 (table ↔ steps) ✓; T2 (interfaces listed before consumers) ✓; T3–T8 each reference lib modules that exist in T1's table ✓ (T3 `hub-terminals`, `equipment-swaps`, `watch-utils`; T4 `metar-explain`, `faa-context`, `nas-severity`, `hub-health`; T5 `special-aircraft`, `analytics`; T6 `starlink-view`; T7 `journey`, `watch-utils`; T8 `engagement`, `tips`, `analytics`) ✓; T11 order from inventory ✓; T12 depends on all ✓.

## Task log
- Task 0: dispatched (opus, main worktree) BASE 7c508e1
- Task 1: dispatched (opus, isolated worktree via Agent isolation) BASE 7c508e1
- Task 0: implementer DONE_WITH_CONCERNS at 213ee73 (46 files; tests 1491/1491; build clean). Concerns: island inline scripts vs CSP; @astrojs/react pinned ^5.0.7 + vite ^7.3.2; Leaflet overrides unverified in browser; Footer shim leaves 11 legacy pages unstyled until T9/T10 (accepted, transitional).
- Ruling: Astro island inline scripts (measured: 3 fixed scripts, sha256 stable per Astro version) are allowed via `'sha256-…'` tokens in vercel.json script-src + a build-time guard `scripts/verify-csp-hashes.mjs`; never `'unsafe-inline'`. Cost if wrong: an Astro upgrade changes hashes → guard fails the build loudly (intended). Plan Task 2 amended (commit after 213ee73).
- Task 0: review dispatched (sonnet) on package review-7c508e1..213ee73.diff
- Task 0: review (sonnet) Approved; Important #1 analytics test lost the mount assertion → fix round 1 dispatched to implementer; Important #2 (plan-mandated) Footer shim leaves 11 legacy pages with an unstyled footer — Ruling: accepted as transitional; MUST be re-verified closed after Tasks 9/10 before the whole-branch review (cost if wrong: contrast regression ships).
- Task 0: minor (deferred): SITE_URL duplicated in Seo.astro / Breadcrumbs.astro / sitemap.xml.ts — centralise when Task 2 adds consumers.
- Task 0: minor (deferred): SiteHeader navKeyForPath startsWith('/news') would match a future /newsletter; re-check at Task 9.
- Task 0: fix round 1/5 (1 addressed, 0 open — analytics test now requires anchored import + real mount; commits 213ee73..8538fc2)
- Task 0: minor (deferred): isMounted accepts a commented-out mount (parity with old test); no leaf-page unmounted-VercelAnalytics mutation case in evidence.
- Task 0: complete (commits 7c508e1..8538fc2, review clean)
- Task 1: implementer DONE at df5ef88 (branch worktree-agent-a8077b1b7ce23e9f3, 4 commits; 22 modules; tests 2018; main.js 8666→7560). Merged into feat/shadcn-rebuild as 550adc4; gates after merge: typecheck 0, tests 2020/2020, build 0. Concerns noted: tierNasEvents returns pre-escaped HTML (React port must not re-escape); bb-visited/bb-onboarded reads now fail open (deviation table in report); build mutates starlink-live.json (revert before commits).
- Task 1: review dispatched (opus) on package review-7c508e1..df5ef88.diff
- Ruling: Task 2 dispatched before Task 1's review verdict (T2 only imports T1's modules; any T1 fix lands in the T1 worktree and is merged after). Cost if wrong: T2 rework on a renamed signature.
- Task 2: dispatched (opus, main worktree) BASE 550adc4
- Ruling: Task 9 dispatched now in an isolated worktree (depends only on T0; files disjoint from T2). Task 10 held until T2 or T9 reports, to cap concurrent Opus implementers at 3 (session rate limit tripped earlier with 2 + reviews). Cost if wrong: idle time.
- Task 9: dispatched (opus, isolated worktree) BASE 550adc4
- Task 1: review (opus) Needs fixes — Important #1 engagement.js fail-open diverges from main.js; Important #2 two undisclosed divergences (engagement.js:88 suppressed folds `submitted`; journey.js:37 undefined→'' vs 'major'). Ruling: revert all three to main.js semantics (zero-behaviour-change contract binds; React port may choose fail-open later, documented). Fix round 1 dispatched to the T1 implementer in its worktree.
- Task 1: minor (deferred): main-js-imports guard misses `const name = (…) =>` re-pastes for 6 un-imported names; comment stripper truncates at `//` inside strings; `shouldShowOnboarding` writes bb-visited (side-effecting predicate name); `createPlaneIcon` builds SVG before the cache check (alloc per marker per poll); `analytics.js` `TYPE_ORDER` exported with no importer (3 copies of the 19-type list remain — consolidate in T5/T12).
- Task 1: facts for later tasks: `AIRPORTS` has 142 entries (not 150); `ICAO_TO_FLEET_TYPE` has 18; `describeFaaProgram` lives in `delay-explain-context.js` (not faa-context.js); several return shapes differ from the brief's table (getPhase→object, decodeSquawk→{text,cls}, estimateRoute→{origin,dest}, indexSpecialAircraft→Map<reg,{name,type}>, networkLabel→{avg,label,color}, avgAgeByType→{rows,fleetAvg}); `buildJourneyContextStr(reg, shaped)`; `flightTimesCacheTtl` has no `ok` param. T3–T8 dispatches must say "read the module exports, not the plan table".
- Task 1: fix round 1/5 (3 addressed pending re-review — engagement fail-open, suppressed `submitted`, journey undefined; commit df5ef88..c7db1ba; merged into trunk as e31485d). Re-review (sonnet) dispatched on review-df5ef88..c7db1ba.diff.
- Task 10: dispatched (opus, isolated worktree) BASE e31485d
- Task 1: fix round 1/5 re-review (sonnet): 3/3 addressed, no new breakage.
- Task 1: complete (commits 7c508e1..c7db1ba on its branch; merged as 550adc4 + e31485d; review clean)
- Task 9: implementer DONE_WITH_CONCERNS at 36e2bd2 (branch worktree-agent-a3ef9ee4deb509093, 4 commits, base 550adc4). Concerns: astro.config `vite.build.assetsInlineLimit: 0` added (Astro was inlining hub-live as an inline module → CSP would refuse in prod); SiteHeader/SiteFooter sub-44px touch targets (T0 files; flagged); `--bb-warn/--bb-ok` hues in content.css pending a shared @theme palette; 29 data-file edits (inline `var(--ua-*)` styles → classes) tabulated.
- Task 9: merge into trunk DEFERRED — trunk worktree has T2's uncommitted BaseLayout.astro edit (merge aborted safely). Merge after T2 commits. Review (opus) dispatched on review-550adc4..36e2bd2.diff.
- Note: `assetsInlineLimit` finding is relevant to T2's CSP guard (inline module scripts must not appear) — T2's verify-csp-hashes.mjs will catch any inlined page script as an unknown hash; that is the intended net.
- Task 9: review (sonnet) Approved, 0 Critical/Important. Verified: JSON-LD counts per page type, no lost meta, 29 data-file edits are style→class only (char-level diff), assetsInlineLimit:0 correct+minimal, TSA box/links removed, scripts ported faithfully, null guards added.
- Task 9: minor (deferred): hubs/index intro paragraphs re-homed into the first ContentSection (cosmetic); `--bb-warn/--bb-ok` in content.css @layer base instead of global.css @theme (consolidate in T12); SiteHeader mobile links 36px and SiteFooter donate button 24px are sub-44px (T0 files — fix in T12 or when T2 touches them).
- Task 9: complete (commits 550adc4..36e2bd2 on its branch, review clean) — MERGE PENDING until T2 commits (trunk worktree dirty with T2's BaseLayout.astro edit).
- Task 10: implementer DONE_WITH_CONCERNS at 288d25f (branch worktree-agent-a9f3969dc05e125ee, base e31485d). Concerns: divergent OG title/description on tracker parent pages dropped (Seo.astro lacked overrides at its base — T9's branch adds ogTitle/ogDescription; restore in a fix round after merge); MAINTENANCE.md og:description clause stale; sticky th pre-existing non-stick. Review (sonnet) dispatched on review-e31485d..288d25f.diff. Merge pending T2.
- Task 9: MERGED into trunk (conflict in astro.config.mjs: T2 envPrefix vs T9 assetsInlineLimit — kept both). Gates after merge recorded below.
- Task 9 merge 2acacbf: typecheck 0, tests 2066/2066, build 0. Task 10 MERGED next (see gates in log). Note from T2: Astro's Vite envPrefix defaults to PUBLIC_ — T2 added `envPrefix: ['PUBLIC_','VITE_']` so VITE_CARTO_BASEMAP_KEY inlines (silent watermark otherwise).
- Task 10 merge b52b1fd: typecheck 0, tests 2066/2066, build 0.
- Task 2: implementer DONE_WITH_CONCERNS at 5a89510 (6 commits on trunk). Concerns: envPrefix fix; /newark + /tsa unstyled until T9(merged now)/T11; HOME_BRIEF keeps 6 bullets (TSA clause edited out of the weather bullet); 4 browser bugs fixed; one now-unused CSP sha256 token to prune in T12. Review (opus) dispatched on review-550adc4..5a89510.diff (range also contains T1's reviewed fix c7db1ba — reviewer told to ignore).
- Controller browser check on trunk b52b1fd (astro dev :4330, /api proxied): Live tab renders 709 flights, hub strip, controls, sidebar, stats bar; /hubs/ord and /trackers/atc on the new system; mobile 400px has bottom nav + one-row controls. CARTO watermark expected (no local key). Console: only the expected dev 404 for /_vercel/insights/script.js. Screenshots: scratchpad/trunk-*.png.
- Task 10: review (sonnet) Approved; Important #1 (plan-mandated) OG/Twitter copy dropped on 5 tracker surfaces → fix round 1 dispatched (merge trunk, use T9's Seo override props); Important #2 MAINTENANCE.md og:description clause stale → in the same fix round.
- Task 10: minor (deferred): map marker r=14 (<44px, justified by cluster overlap); dim-row secondary text tier collapsed; content max-w-4xl vs chrome max-w-5xl (site-wide pattern, revisit T12); sticky th never sticks (pre-existing; needs top-12 + height-constrained container); two defensive `||` fallbacks in trackers.ts.
- Task 10: fix round 1/5 commit ebfb350 merged (ff). Re-review (sonnet) dispatched on review-b52b1fd..ebfb350.diff.
- Task 10: fix round 1/5 re-review (sonnet): 2/2 addressed, no new breakage.
- Task 10: complete (commits e31485d..ebfb350 on its branch; merged b52b1fd + ebfb350; review clean)
- STATUS: complete T0 T1 T9 T10; T2 under review (opus); T3–T8 briefs ready; T11 after T2 lands; T12/T13 last.
- Task 2: review (opus) Needs fixes — 7 Important: #1 home hub never reaches schedule store (useState initializer captures '' before prefs effect); #2 FlightSheet modal makes map inert; #3 no great-circle for estimated routes (route resolution duplicated); #4 (plan-mandated) FR24 lookup affordance dead → Ruling: `FR24_LOOKUP_AVAILABLE=false` flag exported from the placeholder file, palette row hidden + deep-link fallback announces, Task 7 flips it; #5 sheet lacks per-cabin seat blocks + loading/not-in-DB distinction + normalizeWifi; #6 leaflet-required-styles retarget weaker → add src/app scan for fixed+bottom+right-≤12; #7 44px targets on Header/MapControls/mobile Filters. Ruling: three one-line correctness minors folded into the round (VFR squawk red banner, "-7m early", FlightAware UALL). Fix round 1 dispatched to the T2 implementer.
- Task 2: minor (deferred): hex literals in LiveMap/LiveView (export colour map from plane-icon.js, T12); IropsAnnouncer has no IROPS writer (T4 must add announceIropsLevelChange); no visible watch toast (T7/T8); /tsa stylesheet dead + sw.js dead branch (T8/T11); HUB_PROXIMITY_NM doc contradiction; agent-readiness should pin `hideSiteHeader`; LiveMapProps drifted from brief naming; brand link `href="#live"` pushes history; FlightSheet 519 lines; matchLiveFlights per-flight calls; package.json description re-encoded; unpkg still in CSP (T12).
- Task 2 deep-link deviations recorded: `?hub=` selects the board hub instead of typing into search (accepted as better); `?view=` no longer requires `tab=fleet` (harmless); `?type/?filter`/`?view=airborne|special` deferred to T5 — T5 MUST read them from the URL itself or T2's fix must stash them (T5 dispatch says: read `window.location.search` on mount via the deep-links helper); `?tab=irops` scroll pending until T4 consumes `pendingScroll`.
- Ruling: T4 (weather) dispatched now in an isolated worktree at ebfb350 (own files only); T3 waits for T2 fix #1 (schedule store); T5–T8 follow as slots free (cap 2 Opus implementers incl. fix rounds).
- Task 4: dispatched (opus, isolated worktree) BASE ebfb350
- SESSION BREAK (Sep 12 7pm reset → Sep 13 09:00): prior subagents unreachable. T2 fix round 1 left UNCOMMITTED WIP on trunk (10 modified src/app files + new src/app/data/route.ts). T4 left 3 untracked lib modules + tests in its worktree (irops-client, tracker-briefing, weather-cards). Fresh Opus implementers dispatched to continue both from disk state (rounds 1–3 rule: fresh implementer carries brief + report + findings).
- Task 2: fix round 1/5 implemented by a fresh implementer (commits ebfb350..2294856: 6118a61 findings 1–5 + 3 minors; d92bbc8 finding 6; a64ac9b finding 7; 2294856 follow-ups). Gates: 2068 tests, typecheck 0, build 0 (CSP guard: 2 inline scripts allowed). Corrections to prior WIP: zoom-control offset rule fixed (−8rem ≥1024, −28rem ≥640); CommandEmpty regression restored; extra sub-44px targets fixed (Starlink toggle, search button, sheet ✕ 44×44 via sheet.tsx + global.css — outside enumerated files, additive). vercel.json reverted (dead-hash prune is T12). Re-review (sonnet) dispatched on review-ebfb350..2294856.diff.
- Task 2: fix round 1/5 re-review (sonnet): 8/8 addressed (7 Important + folded minors), no new breakage. sheet.tsx `showOverlay` prop + 44×44 close are app-wide additive.
- Task 2: complete (commits 550adc4..2294856 on trunk, review clean). Trunk HEAD 2294856.
- Task 3: dispatched (opus, isolated worktree) BASE 2294856
- Task 4: implementer DONE at fc5cf57 (branch worktree-agent-a163a2593e3874bcf, 5 commits, base ebfb350; tests 2142). Concerns: IROPS client fallback + extra METAR stations wired to `useSchedule().boards` (still a stub until T3) — re-check after T3 merges; additive edits: state/irops.tsx (+hubRates/score/reportClientScore), shell/Ticker.tsx (1 line), shell/IropsAnnouncer.tsx (owns level-change announcement). Merged into trunk (see gates below). Review (sonnet) dispatched on review-ebfb350..fc5cf57.diff.
- Task 4 merge b87d7f8: typecheck 0, tests 2144/2144, build 0 (CSP guard 2 allowed).
- Task 5: dispatched (opus, isolated worktree) BASE b87d7f8. Running: T3 (schedule), T5 (fleet); review T4 (sonnet).
- Task 4: review (sonnet) Needs fixes — Important #1 44px targets missing on retry button (HubCards.tsx:65), IROPS "?" trigger (IropsSection.tsx:169), Details summary (HubCard.tsx:148) → fix round 1 dispatched to the T4 implementer.
- Task 4: minor (deferred): merge-not-replace fetch semantics → total-failure retry state reachable only on cold start; stale FAA program persists after a failed /api/faa cycle (no expiry) — consider an expiry in T12 (legacy likely same); RadarMap bindHubTooltip unbind/rebind vs setContent.
- Task 4: fix round 1/5 commit d751cc6 merged (ff). Re-review (sonnet) dispatched on review-fc5cf57..d751cc6.diff.
- Task 4: fix round 1/5 re-review (sonnet): 1/1 addressed, no new breakage (note: fix1-400-help.png crop did not show the "?" — verdict stands on CSS classes).
- Task 4: complete (commits ebfb350..d751cc6 on its branch; merged b87d7f8 + 5c248cb; review clean). Trunk HEAD 5c248cb.
- STATUS: complete T0 T1 T2 T4 T9 T10; running T3 (schedule) + T5 (fleet); queued T6 T7 T8 T11 T12 T13.
- Rate-limit break (2pm reset; resumed 20:47 Sep 13). T3 on disk: 083a65e + 7964acc + uncommitted SearchPalette wiring (browser pass pending). T5 on disk: 56ee2ef + b889eb1 + fa0a8d0 + uncommitted dialog focus fix. Both agents resumed via SendMessage.
- Task 5: implementer DONE at 9c4c419 (5 commits, base b87d7f8; tests 2178). Concerns: dialog.tsx close 44×44 (shared, precedented); sub-tab activationMode + Escape reasoned not key-proven (harness has no key events); loadFailed branch unexercised; four `!` modifiers (T12); Starlink mainline % denominators differ (legacy parity). Merged (see gates). Review (sonnet) dispatched on review-b87d7f8..9c4c419.diff.
- Task 6: dispatched (opus, isolated worktree) BASE = trunk after T5 merge.
- Task 3: implementer DONE at f95c844 (5 commits, base b87d7f8; tests 2185, +41 schedule-load). Concerns: BMAC landed toast deferred to T8 (no hook); WatchBanner mounted in ScheduleView (belongs in shell — T8 to relocate); additive `updateStatus()` on state/watch.tsx + SearchPalette result group; useHubHealth now depends on useSchedule (must be under ScheduleProvider); swap detection runs only on real fetch (deliberate improvement); dev proxy spends AeroDataBox units. Merged (see gates). Review (sonnet) dispatched on review-b87d7f8..f95c844.diff.
- Task 7: dispatched (opus, isolated worktree) BASE = trunk after T3 merge.
- Task 5: review (sonnet) Needs fixes — Important #1 FleetControls.tsx five sites use `sm:` (640) instead of `md:` for the 44→32 px transition; Important #2 load-failure state blanks the whole tab (legacy kept the feed-driven Zone 1 pulse). Fix round 1 dispatched to the T5 implementer.
- Task 5: minor (deferred): `#22c55e` fallback literal in AircraftDetailDialog.tsx:115 (derive from FLEET_HEALTH_CATEGORIES, T12); FleetView 430 / AircraftDetailDialog 428 lines; row-dim edge case stored+special; QUICK_TYPES copy in TSX.
- Rate-limit break (9:30pm reset; resumed 22:05 Sep 13). T5 fix committed 58b5f5b (report section pending); T6 e401ed4 + WIP roster; T7 lib WIP (also modifies src/lib/delay-risk.js — reviewer to check additivity); T3 review agent resumed to write its report. All four resumed via SendMessage.
- Task 5: fix round 1/5 commit 58b5f5b merged; also fixed a false 'regional/partner' subtitle in the failure state. Re-review (sonnet) dispatched on review-9c4c419..58b5f5b.diff.
- Task 3: review (sonnet) Needs fixes — Important #1 autoScrollKey not board-scoped (straggling load can scroll the currently viewed board); Important #2 (plan-mandated) useBoardModel.ts 726 lines of untested pure row logic → extract to src/lib with tests. Fix round 1 dispatched to the T3 implementer. Review also verified: swap-detection-on-real-fetch is a strict improvement (legacy wiped badges on cache hits); goto() not clearing filters = legacy parity; hub-health frozen serverNowMs has no effect on OTP.
- Task 3: minor (deferred): legacy "⚡ Served from cache" message dropped (cosmetic); ScheduleTable.tsx 481 lines; otpTone threshold in TSX (folded into fix #2).
- Task 5: fix round 1/5 re-review (sonnet): 2/2 addressed, no new breakage.
- Task 5: complete (commits b87d7f8..58b5f5b on its branch; merged f76b819 + 8253a0a; review clean). Trunk HEAD 8253a0a.
- STATUS: complete T0 T1 T2 T4 T5 T9 T10; T3 fix round 1 running; T6 T7 implementers running; queued T8 T11 T12 T13.
- Task 11: dispatched (sonnet — mechanical, inventory-specified; isolated worktree) BASE 8253a0a
- Task 3: fix round 1/5 commit 48becd6 merged (schedule-row-model.js +55 tests; autoscroll board-scoped). Re-review (sonnet) dispatched on review-f95c844..48becd6.diff.
- Task 11: implementer (sonnet) DONE at b455f66 (22 files: 7 deletions, 15 edits; /tsa → /hubs 301 added). Merged (see gates). Review (sonnet) dispatched on review-8253a0a..b455f66.diff.
- Task 11 merge bfea262: typecheck 0, tests 2257/2257 (123 files; the two TSA suites deleted by design), build 0, dist/tsa.html absent. Trunk HEAD bfea262.
- Running: T3 re-review, T11 review (sonnet); T6 + T7 implementers (opus). T8 dispatches when an Opus slot frees.
- Rate-limit break (3am reset; resumed 08:50 Sep 14). T7 on disk: 3 commits (5f46043 f4637cb d874390), browser pass pending. T6 on disk: e401ed4 + c699936 + staged starlink views (re-verify after closure refactor). T3 re-review + T11 review agents died before output — resumed. All four resumed via SendMessage.
- Task 11: review (sonnet) Approved, 0 findings (audit grep reconciled: 17 hits all keep-list/false-positive/newer docs). Author confirmed via git log (see command output). Minor: report tally 22 vs 23 files.
- Task 11: complete (commit b455f66 on its branch; merged bfea262; review clean).
- Task 3: fix round 1/5 re-review (sonnet): 2/2 addressed, no new breakage.
- Task 3: minor (deferred): autoScroll is a single latest-signal slot (a superseded board's one-shot anchor can be missed — safe direction); ident fallback '' vs '—' duplication in useBoardModel.ts:408-419; trivial displayCls wrapper.
- Task 3: complete (commits b87d7f8..48becd6 on its branch; merged 1ca5d08 + cd53c8c; review clean).
- STATUS: complete T0 T1 T2 T3 T4 T5 T9 T10 T11; running T6 + T7 implementers; queued T8 T12 T13.
- Task 6: implementer DONE at 6050f06 (3 commits, base f76b819; tests 2242, +64). Concerns: degraded tier verified by inspection only; #sl-search un-debounced (legacy parity); board default hub = home airport (legacy: ALL) — divergence flagged, Ruling: keep home-airport default (consistent with Schedule); velocity chart scrolls <620px. Additive: state/ui.tsx starlinkFilter, LiveView reads it. Merged (see gates). Review (sonnet) dispatched.
- Task 8: dispatched (opus, isolated worktree) BASE = trunk after T6 merge.
- Task 6: review (sonnet) Approved, 0 Critical/Important (verified verbatim strings/formulas vs legacy; no §22 rule in TSX; degraded tier + one-shot ledger fetch hold).
- Task 6: minor (deferred): StarlinkView.tsx 502 lines (extract ledger-fetch effect / pace hook); industry-strip footnote is new copy (not legacy); onBoardTrack identity churns with feed.flights.
- Task 6: complete (commits f76b819..6050f06 on its branch; merged b7b6557; review clean).
- STATUS: complete T0 T1 T2 T3 T4 T5 T6 T9 T10 T11; running T7 (verification) + T8; queued T12 T13.
- Task 7: implementer DONE at b366457 (4 commits, base 1ca5d08; tests 2360, +147 in 7 files; FR24_LOOKUP_AVAILABLE=true). Concerns: /api/flight-times has no gate times for airborne flights today (upstream) so countdown/gate paths verified by tests; push grant + F048 render not headless-exercisable; cold session shows RISK N/A until boards load; WatchBanner still Schedule-only (T8). Merged (see gates). Review (sonnet) dispatched.
- Task 7 merge 5dd1cc9: typecheck 0, tests 2462/2462 (132 files), build 0. Review (sonnet) dispatched on review-1ca5d08..b366457.diff.
- Ruling: Task 12 dispatched now in an isolated worktree while T8 still runs — T12's file set (deletions of src/dashboard, legacy, public/js|css|fonts; vercel.json; csp/asset-cache tests; docs; version) is disjoint from T8's (src/app, sw.js, manifest, sw/popup-triggers tests). The plane-icon colour-map debt (src/app) is explicitly deferred out of T12. Cost if wrong: a merge conflict I resolve.
- Task 12: dispatched (opus, isolated worktree) BASE 5dd1cc9
- Task 7: review (sonnet) Needs fixes — Important #1 FlightCard risk-badge button under 44 px (report claimed zero). Fix round 1 dispatched (also restore `#myflight-empty` id). Verified: delay-risk.js change additive (RISK_BANDS export only); FR24 leg metadata fix matches api/fr24-flight.ts; all API bodies byte-exact.
- Task 7: minor (deferred): PROMPT_DELAY_MS/PROMPT_VISIBLE_MS and journey TTL_MS inline in TSX (non-branching literals).
- Rate-limit break (1:40pm reset; resumed 13:45 Sep 14). T7 fix: 3 files edited, uncommitted (badge 57×44 measured). T8: 4 commits done (746d754 = required subject), browser pass pending. T12: deletions committed 97cf77b. All three resumed.
- Task 7: fix round 1/5 commit 3c033e5 merged (badge 57×44; 3 more WatchPanel targets fixed; #myflight-empty restored). Re-review (sonnet) dispatched on review-b366457..3c033e5.diff.
- Task 7 fix merged as b54e12d (after restoring the tracked report copy): typecheck 0, tests 2462/2462. FOUND: `.superpowers/sdd/…/task-7-report.md` is TRACKED (committed by the T7 branch; `.superpowers/` not in .gitignore). Ruling: T12 untracks it (`git rm --cached -r .superpowers`) and adds `.superpowers/` to .gitignore; T13 verifies `git ls-files .superpowers` is empty before the PR.
- Task 7: fix round 1/5 re-review (sonnet): all addressed, no new breakage.
- Task 7: complete (commits 1ca5d08..3c033e5 on its branch; merged 5dd1cc9 + b54e12d; review clean).
- STATUS: complete T0 T1 T2 T3 T4 T5 T6 T7 T9 T10 T11; running T8 (browser pass) + T12 (cleanup); queued T13.
- Task 8: implementer DONE at c5c957e (5 commits, base b7b6557; tests 2360 in-worktree). Deviations reported: phase-donut hue ramp re-stepped (legacy 3 near-identical blues failed the dataviz validator; §25 pins geometry not hues) — Ruling: ACCEPT; ⓘ legal menu + BMAC toast moved out of the map's bottom-right (legacy overlapped Leaflet zoom-out; leaflet test forbids it) — Ruling: ACCEPT; engagement store is a module singleton (Dashboard.tsx closed); `?waitlist=1` on fresh profile stacks onboarding over waitlist (legacy parity, pinned). Merged (see gates). Review (sonnet) dispatched.
- Task 8 merge 6c50fec: typecheck 0, tests 2501/2501 (134 files), build 0 (CSP guard 2 allowed). Trunk HEAD 6c50fec. Every dashboard tab is now on the trunk. Remaining: T8 review; T12 (running, must merge trunk incl. T8 before its final commit); T13.
- Task 12: implementer DONE at 9706d5e (6 commits incl. 2 trunk merges; base 5dd1cc9). Trunk fast-forwarded to 9706d5e. Gates: typecheck 0, tests 2380/2380 (133 files — main-js-imports guard deleted by design), build 0, CSP guard 2 allowed, no legacy/public-js/css/fonts/src-dashboard tracked, no unpkg, version 1.8.0, .superpowers untracked + ignored. Live concern: `HUB_PROXIMITY_NM = 93` compared against haversineNm → real radius 93 NM not the documented ~50 nm (legacy bug preserved byte-for-byte; Jonah's call — list in PR). Review (sonnet) dispatched on review-6c50fec..9706d5e.diff.
- Task 13: parity audit dispatched (opus, fresh reviewer, trunk worktree, writes docs/superpowers/specs/2026-09-11-parity-audit.md).
- Task 12: review (sonnet) Approved, 0 Critical/Important (CSP hashes re-derived from dist; vercel.json rules; site.js wiring; DESIGN.md claims spot-checked; ls-files clean). Minor: CHANGELOG says "nine" colour modules, DESIGN.md table lists ten → folded into the T8 fix round (F3). Minor (accepted): `/fonts/` remains in site-routes.js ASSET_PREFIXES (fail-safe classification) and a past-tense sw.js comment.
- Task 12: complete (commits 5dd1cc9..9706d5e; review clean).
- Task 8: review (sonnet) Needs fixes — Important #1 util/age bar fills (`UTIL_BAR_COLOR`/`AGE_BAR_COLOR`) deviate from §25's pinned hex and the file header wrongly blames `--ua-*` vars (legacy used literal hex). Ruling: ACCEPT the palette deviation (extends the donut ruling: §25 pins geometry + thresholds, not hues; thresholds are byte-for-byte), correct the header, record the legacy hex. Cost if wrong: 8 constants to revert. Important #2 `?waitlist=1` stacking — the report's "parity" claim was wrong: legacy waitlist z 10001 > onboarding 10000, port renders onboarding on top. Ruling: fix (waitlist dialog + overlay `z-[60]`, `overlayClassName` prop on DialogContent, source-scan test). Important #3 Onboarding hub picker seeded once at mount → reopening via "?" after a header hub change writes the stale hub back on dismiss (regression vs legacy, which never pre-populated). Ruling: fix (sync on open). Fix round 1/5 dispatched (opus, isolated worktree, brief task-8-fix-brief.md) — 2 Opus slots in use (fix round + parity audit).
- Task 8: minor (deferred): engagement.tsx singleton store has no direct unit test; ROTATE_MS/START_DELAY_MS inline in NewsBanner/TipStrip (non-branching literals).
- Task 13 (mine): browser QA on the trunk dev server (:4334): all 8 tabs at 1440 and 400 px render with live data; ⌘K opens; `?flight=UA1` opens the sheet (desktop + mobile); `?tab=irops` scrolls to #irops-section; `?hub=den&tab=schedule` selects Schedule/DEN; `?waitlist=1` opens the waitlist dialog (stacking bug above confirmed here too); /hubs /hubs/ord /fleet /fleet/737-800 /news /newark /privacy /trackers/atc /404 render with header/footer/breadcrumbs; scrollWidth == innerWidth at 400; console: only the dev-only `/_vercel/insights/script.js` 404; no non-2xx requests. "API KEY REQUIRED" tile watermark is the expected no-local-key state. Bottom-centre pill in screenshots = Astro dev toolbar.
- Task 13: parity audit agent died at the 6:40pm session limit mid-verification; resumed 19:00 via SendMessage.
- Housekeeping: removed untracked `public/js/dashboard.js` (Sep 12 artifact of the deleted vite dashboard build).
- Ruling 9 (Footer shim transitional) CLOSED: no importers after T9/T10/T12; shim deleted on the trunk (b07bdce); typecheck 0, tests green.
- Final whole-branch review dispatched (sonnet, read-only on trunk b07bdce; brief final-review-brief.md; T8 fix findings + HUB_PROXIMITY excluded). PR screenshots staged under docs/superpowers/specs/2026-09-11-screenshots/ (6 jpg).
- Gate at 0f31331 (shim deletion + screenshots): typecheck 0, tests 2380/2380 (133 files), build 0, CSP guard 2 allowed; tree clean.
- Task 8: fix round 1/5 DONE at a5ac70d on fix/task-8-review (R1 header, F1 z-[60] + overlayClassName + 7 tests, F2 onboardingHubSeed + effect + tests, F3 CHANGELOG; gates 134 files / 2391 tests, build + CSP clean). Implementer found z-index does not govern Radix's modal stack: on fresh ?waitlist=1 onboarding still traps focus and the first click dismisses onboarding. Ruling: keep z-[60]; Onboarding defers while the waitlist dialog is open and opens after it closes (legacy effective UX; pure helper + test). Round 2 dispatched to the same agent. Cost if wrong: welcome dialog shows one click later for ?waitlist=1 arrivals only.
- Final review (sonnet): Approved, 0 Critical / 0 Important / 2 Minor (trackers.ts literal storage keys; NewsBanner rotating role=status). Both fixed on the trunk 37f9980 as the single final fix wave (+ DESIGN.md live-region inventory). schedule.test.js 'repeated later-page rate limits' flaked once under the full suite, passes in isolation (57/57) — timing-sensitive API test, pre-existing.
- Task 8: fix rounds 1–2 (a5ac70d, 9630703) merged ea91c5b (10 files +344/−12; implementer added a once-per-load autoOpened guard after reproducing a welcome-returns regression — accepted). Gates: typecheck 0, tests 2398/2398 (134 files). Re-review (sonnet) dispatched on review-9706d5e..9630703.diff.
- Final review CORRECTED by the reviewer: Needs fixes — Important: 7 overlays (FlightSheet, AircraftDetailDialog, DelayExplainDialog, Fr24LookupDialog, Onboarding, DisclaimerDialog, WaitlistDialog) open via state setters with no Radix Trigger; Radix's default onCloseAutoFocus preventDefaults then focuses a null triggerRef → focus drops to body on close (WCAG 2.4.3). Verified in node_modules/@radix-ui/react-dialog. Ruling: fix once in the shared ui/dialog.tsx + ui/sheet.tsx wrappers (capture opener on content mount; on close restore it only if focus was lost, so a non-modal sheet closed by clicking the map does not yank focus back); pure rule in src/lib with tests. Dispatched to the T8 fix implementer as fix/final-review-focus off ea91c5b. Cost if wrong: focus restore fires in a case the user moved focus deliberately (non-modal sheet only).
- Task 13 parity audit on disk (uncommitted by the agent yet): 551 rows — 422 ✅ / 68 🔁 / 11 ❌ marks = 7 distinct gaps, 0 user-visible feature loss. G2 already closed (shim). Ruling: fix G1 (legacy watchedFlights fallback — weather preload only, as legacy), G3 (restore '⚡ Served from cache' board message; the T3 deferred minor was not an ACCEPT), G4–G7 (docs/dead prefixes). Dispatched as fix/parity-gaps (opus, isolated worktree, brief task-13-gaps-brief.md) concurrently with the focus round — 3 Opus agents briefly while the audit agent finishes its commit.
- Task 8: fix rounds re-review (sonnet): 4/4 addressed (R1 hex verified against the deleted main.js; F1 two-part; F2; F3), no new breakage, gates 134/2398. Task 8 COMPLETE (review clean).
- Task 13: parity audit committed by the audit agent as d88ddb7 (docs/superpowers/specs/2026-09-11-parity-audit.md).
- Final-review Important (focus return): fixed at 72ea0b8 on fix/final-review-focus, cherry-picked onto the trunk as 8f1bd70 (the branch also carried the superseded audit commit 5ffb5f4, so no merge). Implementer deviation accepted: the capture lives in a CaptureOpener child inside the Radix content (the wrapper never unmounts — measured, not guessed); content ref composed. Measurements: help-button and watch-panel openers regain focus; palette-opened sheet → body (opener unmounted, rule declines); non-modal sheet closed with focus on the map → focus stays on the map. Gates: typecheck 0, tests 2410/2410 (135 files). Re-review (sonnet) dispatched on review-d88ddb7..8f1bd70.diff.
- Task 13 gaps: fix/parity-gaps (6 commits b652f75..177286d) merged b3fe969: G1 legacy watchedFlights fallback gated on the RAW key being absent (legacy || semantics — '[]' is truthy), weather preload only; G3 boardLoadMessage() + Board.fromCache (wasCached || result.cached) rendered under StalenessBanner; G4 TODOS; G5 comment; G6 DESIGN.md unpkg; G7 dead prefixes → matched list, /_agent/ added to ASSET_PREFIXES (inert, safe direction). Gates: typecheck 0, tests 2418/2418 (135 files). CLAUDE.md CSP summary corrected (names the analytics beacon) 7be1629. Review (sonnet) dispatched on review-ea91c5b..177286d.diff.
- Trunk smoke (dev :4334) at 70b15f2: ?waitlist=1 → only the waitlist dialog, focus in #waitlist-email; Escape → onboarding, focus on hub select. Board reload → '⚡ Served from cache · 659 UA flights' rendered. Focus return: trusted click + Escape → help button (dialog) and 'Watched flights (0)' button (sheet) regain focus; a synthetic focus()+click() in one evaluation showed BODY — test artifact, not a bug. Build gate at 7be1629 clean (CSP guard 2 allowed); 70b15f2 is docs-only on top. Parity audit resolution table committed 70b15f2.
- Focus-return re-review (sonnet): clean (timing by React effect ordering; composeEventHandlers semantics; composed ref; non-modal path). Minor (deferred): the insideClosingContent clause of wasFocusLost is unreachable as wired (Radix dispatches onCloseAutoFocus from a setTimeout after unmount, when the ref is null and focus is already body — the isBody clause does the work); comment/test describe a mechanism the integration never exercises. Gates 2418/2418.
- Parity-gap review (sonnet): 6/6 addressed, no breakage, no scope creep. Task 13 COMPLETE.
- FINAL GATES at 70b15f2: typecheck 0, tests 2418/2418 (135 files), build 0, CSP guard 2 allowed, ls-files clean of legacy paths, version 1.8.0, 361 files +43,829/−18,110 vs main. Dev server stopped. Ledger copied to docs/superpowers/specs/2026-09-11-sdd-ledger.md for the hand-off; SDD workspace deleted after the PR.
- Sep 16 2026 — External review (ChatGPT) of PR #254 reproduced three state-lifecycle regressions in glue code the pure-logic suite could not see: (P1) stale tab's whole-list persist deletes another tab's watch edits (legacy re-read storage per op); (P2) batched updateStatus calls derive from one stale ref, last wins; (P2) Starlink ledger guard latched before the fetch, never reset after a tab-switch cancel. Ruling: fix wave 2 on the same PR; allow exactly two devDeps (@testing-library/react 16.3.3, jsdom 30.0.1) for provider-level React tests, default vitest env stays node (per-file opt-in). Ruling on HUB_PROXIMITY_NM: keep 93 in this PR; fix the rule (route-first, then location assignment) in a separate change — a 50 NM radius still admits SJC-from-SFO and DCA-from-IAD. Fix wave 2 = 138980c (red tests) + 21abfd8 (F1) + 0c07a33 (F2) + c6bebe6 (F3), merged cfca526. Gates: typecheck 0, tests 2444/2444 (138 files). Lesson recorded: add integration checks for state ownership and lifecycle transitions; pure-function counts do not cover them.
- Sep 18 2026 — Fix wave 2 review (sonnet): clean — F1/F2/F3 addressed, diffWatched announcement block byte-for-byte untouched, tooling limited to the two pinned devDeps, no direct-dependency change in bun.lock. Gates at 0262754: typecheck 0, tests 2444/2444 (138 files), build 66 pages, CSP guard 2 allowed. Follow-ups from the external review tracked in TODOS.md (stale FAA state, hub proximity rule, focus fallback, engagement lifecycle tests). (The first review attempt died at the weekly limit on Sep 16; a git Xcode-license error blocked one docs commit — both resumed Sep 18.)
